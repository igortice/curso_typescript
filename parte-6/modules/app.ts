import * as Circle from './math/circle'; // import { PI, calculateCircumference } from './math/circle';
import calcRect from './math/rectangle';

console.log(Circle.PI);
console.log(Circle.calculateCircumference(10));
console.log(calcRect(20, 50));
